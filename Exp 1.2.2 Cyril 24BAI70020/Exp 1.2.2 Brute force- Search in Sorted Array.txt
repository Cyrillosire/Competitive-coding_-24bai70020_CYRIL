class Solution:
    def SearchinSortedArray(self,nums,target):
        for i in range(len(nums)):
            if nums[i]==target:
                return i
            
        return -1
            
sol= Solution()
nums=[1,2,4,5,6]
target=7
print(sol.SearchinSortedArray(nums,target))